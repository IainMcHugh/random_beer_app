import React from "react";

import { Navbar, LogoWrapper, Logo } from "./styles";

const Navigation = () => (
  <Navbar>
    <LogoWrapper>
      <Logo />
    </LogoWrapper>
  </Navbar>
);

export default Navigation;
